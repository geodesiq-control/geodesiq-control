[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "geodesiq"
dynamic = ["version"]
description = "A Python package for computing optimal quantum control pulses using the geodesic approach."
readme = "README.md"
license = "LGPL-2.1-only"
license-files = ["LICENSE"]
authors = [
    { name = "David Fernández Fernández", email = "david.fernandezf@hotmail.com" },
    { name = "Christian Ventura Meinersen", email = "C.VenturaMeinersen@tudelft.nl" }
]
requires-python = ">=3.10"
keywords = ["quantum-control", "optimal-control", "geodesic", "quantum-computing", "python", "spin-qubits",
    "superconducting-qubits", "trapped-ions", "quantum-dots"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering",
    "Typing :: Typed",
]
dependencies = [
    "numpy>=1.23",
    "scipy>=1.15",
    "qutip>=5.0.4",
]

[project.urls]
Homepage = "https://github.com/geodesiq-control/geodesiq-control"
Documentation = "https://github.com/geodesiq-control/geodesiq-control/tree/main/docs"
Repository = "https://github.com/geodesiq-control/geodesiq-control"
Issues = "https://github.com/geodesiq-control/geodesiq-control/issues"
Changelog = "https://github.com/geodesiq-control/geodesiq-control/blob/main/CHANGELOG.md"

[project.optional-dependencies]
plot = ["matplotlib>=3.9"]

[dependency-groups]
test = [
    "pytest>=8,<9",
    "pytest-cov>=5,<7",
]
lint = [
    "mypy>=1.10",
    "ruff>=0.11",
]
build = [
    "build>=1.2",
    "packaging>=24",
    "twine>=6",
]
bench = [
    "filelock>=3.15",
    "ipython>=8.0",
    "ipywidgets>=8.1",
    "matplotlib>=3.9",
    "pandas>=2.0",
    "pyarrow>=15",
    "tqdm>=4.68",
]
notebooks = [
    "ipykernel==6.29.5", # Pinned because newer releases have caused ipywidgets regressions.
    "ipympl>=0.10.0",
    "joblib>=1.5.2",
    "jupyterlab>=4.5.8",
    "jupyterlab-execute-time>=3.3.0",
    "theme-darcula>=4.0.0",
    "tqdm>=4.68.2",
    "parameterrun>=0.4.1",
    "quspin>=1.0.0",
]
dev = [
    { include-group = "test" },
    { include-group = "lint" },
    { include-group = "build" },
    { include-group = "bench" },
    { include-group = "notebooks" },
]

[tool.hatch.version]
path = "src/geodesiq/_meta.py"
pattern = '__version__ = "(?P<version>[^"]+)"'

[tool.hatch.build.targets.wheel]
packages = ["src/geodesiq"]

[tool.hatch.build.targets.sdist]
include = [
    "/src",
    "/README.md",
    "/LICENSE",
    "/CHANGELOG.md",
]

exclude = [
    "/.github",
    "/.idea",
    "/.vscode",
    "/benchmarks",
    "/dev",
    "/docs",
    "/examples",
    "/images",
    "/tests",
    "/dist",
]

[tool.uv]
package = true
default-groups = ["dev"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --cov=geodesiq --cov-report=term-missing --cov-fail-under=70"

[tool.ruff]
target-version = "py310"
line-length = 120
src = ["src"]
extend-exclude = [
    "dev",
    "examples",
]


[tool.ruff.lint]
select = ["E", "F", "B"]

[tool.mypy]
python_version = "3.10"
files = ["src"]

warn_unused_ignores = true
warn_redundant_casts = true
disallow_untyped_defs = true
ignore_missing_imports = true
check_untyped_defs = true
no_implicit_optional = true